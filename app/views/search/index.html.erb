<!-- render search results -->
<% if @arts.present? || @artists.present? || @galleries.present? %>
  <%= render '/arts/results' %>
<% else %>
  <p>There are no posts containing the term(s) <%= params[:search] %>.</p>
<% end %>
