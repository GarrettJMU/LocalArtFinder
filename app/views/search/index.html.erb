<!-- render search results -->
<% if @arts.present? || @artists.present? || @galleries.present?  || @events.present? %>
  <%= render '/search/results' %>
<% else %>
  <p>There are no posts containing the term(s) <%= params[:search] %>.</p>
<% end %>
